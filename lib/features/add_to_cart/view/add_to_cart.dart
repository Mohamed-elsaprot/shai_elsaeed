import 'package:flutter/material.dart';

import 'add_to_cart_body.dart';

class AddToCart extends StatelessWidget {
  const AddToCart({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const AddToCartBody();
  }
}
